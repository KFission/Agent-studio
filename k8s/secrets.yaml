# JAI Agent OS — Secrets (placeholder — use GKE Secret Manager in production)
# In production, use: gcloud secrets or External Secrets Operator
apiVersion: v1
kind: Secret
metadata:
  name: jai-api-keys
  namespace: jai-agent-os
  labels:
    app: jai-backend
type: Opaque
stringData:
  # Replace with actual keys or use External Secrets Operator
  GOOGLE_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  LANGSMITH_API_KEY: "REPLACE_ME"
  TAVILY_API_KEY: "REPLACE_ME"
  KEYCLOAK_CLIENT_SECRET: "REPLACE_ME"
